#
# after_print_stats
#
#	A freewrite after function to print statistics from the session file.
#	The statistics are gathered in the after_collect_stats function file.
#

after_print_stats () {
	echo "You wrote ${session_data["word_count"]} words in ${session_data["writing_time"]}. Your defined pace is $writing_pace words in $writing_time minutes."
	echo "This session is ${session_data["pace_status"]}."
}