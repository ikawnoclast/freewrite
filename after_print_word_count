#
# after_print_word_count
#
#	A freewrite after function to determine the word of the session file.
#	The initial file loaded by the editor contains a few lines from a 
#	template file that must be removed so that an accurate word count 
#	can be generated.
#
# Keith A. Watson <ikawnoclast@gmail.com>
#

after_print_word_count () {
	local word_count=$(sed '1,/^-*/d' < $session_file | wc -w | \
			   awk '{print $1}')
	echo "You wrote $word_count words."
	echo "word_count: $word_count" >> $data_file
}